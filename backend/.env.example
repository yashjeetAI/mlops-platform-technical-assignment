# Copy to .env and adjust as needed.
APP_NAME=MLOps Platform
ENVIRONMENT=local
# Postgres is the app database (Docker Compose provides it). Tests use SQLite.
DATABASE_URL=postgresql+psycopg://mlops:mlops@localhost:5432/mlops
LOG_LEVEL=INFO
