[project]
name = "mlops-platform-backend"
version = "0.1.0"
description = "MLOps control-plane backend (registry, deployments, monitoring)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
